{
  "name": "nusp1-documentation",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "organize": "node scripts/organize-docs.js && node scripts/fix_paths.js",
    "build": "npm run organize",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d docs"
  },
  "devDependencies": {
    "vite": "^7.1.7"
  },
  "dependencies": {
    "gh-pages": "^6.3.0"
  }
}
