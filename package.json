{
  "name": "nusp1-documentation",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "organize": "node scripts/organize-docs.js",
    "dev": "npm run organize && vite",
    "build": "npm run organize && vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^7.1.7"
  },
  "dependencies": {
    "gh-pages": "^6.3.0"
  }
}
